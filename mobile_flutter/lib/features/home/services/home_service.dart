// HomeService — Service
// Fetches all data needed by the home screen.
// Methods: fetchAnnouncements(), fetchGlanceItems()
// Currently uses mock data — RADJA* replaces with real API calls.
// TODO (Radja): replace mock data with GET /announcements and GET /bookings/