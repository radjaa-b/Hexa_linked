// GlanceItemModel — Model
// Holds the data shape of one glance card shown on the home screen.
// Fields: title, subtitle, icon, type (booking / request / visitor)

// TODO (Radja): map your API JSON response to this model